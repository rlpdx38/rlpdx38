# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## About This Repository

`rlpdx38/rlpdx38` is a GitHub special profile repository. Its `README.md` appears as the profile README on the GitHub account page at `https://github.com/rlpdx38`. There is no application code, build system, or test suite — the sole artifact is `README.md`.

## Working with This Repository

- The only file to edit is `README.md` (Markdown).
- Changes are immediately reflected on the public GitHub profile once pushed to `main`.
- There are no dependencies to install, no build steps, and no tests to run.

## README Content

The current `README.md` contains only the default GitHub profile template with all content commented out. To update the profile, replace or fill in the template placeholders in `README.md` and push to `main`.
